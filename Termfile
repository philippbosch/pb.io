# WestOst App

PROJECT_NAME = "pb.io"

setup do
  run "bundle install"
end

before do
  run "cd ~/Sites/#{PROJECT_NAME}"
end

tab "#{PROJECT_NAME}" do
  pane do
    run "foreman start"
    
    pane do
      run "mate ."
      run "open http://localhost:4000/"
      run "clear && ls -alG"
    end
  end
end
